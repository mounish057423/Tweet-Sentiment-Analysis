# server {
#     listen 80;

#     server_name localhost;

#     root /usr/share/nginx/html;
#     index index.html;

#     location / {
#         try_files $uri /index.html;
#     }

#     # 🔥 THIS FIXES YOUR API CALLS
#     location /sentiment/ {
#         proxy_pass http://backend:8000/sentiment/;
#         proxy_http_version 1.1;

#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#     }
# }
server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    # ← ADD THIS - fixes MIME type for JS/CSS files
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2)$ {
        try_files $uri =404;
        add_header Cache-Control "public, max-age=31536000";
    }

    location / {
        try_files $uri /index.html;
    }

    location /sentiment/ {
        proxy_pass http://backend:8000/sentiment/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}

## Error 2 — Backend ReadTimeout (500 error)
